<h2> Your History of Tenants </h2>

<!-- this isnt good -->
<p>

<% @current_connections.each do |connection| %>
	<%= connection.start_date.strftime("%B %e, %Y ") %> </br>
	<%= connection.end_date.strftime("%B %e, %Y ") %> </br>
	<%= connection.apartment.apartment_name %> </br>

	<!-- should be @tenants -->
	<% Tenant.all.each do |tenant| %>
		<%= tenant.full_name %> </br>
		<%= tenant.email %> </br>
		<%= tenant.phone_number %> </br> 
	<% end %>
	<hr>
<% end %>
</p>